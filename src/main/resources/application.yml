server:
  port: 8086

spring:
  datasource:
    platform: mysql
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/invoicing-system?createDatabaseIfNotExist=true
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL5Dialect